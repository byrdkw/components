$('body')
  .on('click', 'button.btn-search', function(event) {
    event.preventDefault();
    var $input = $('input');
    $input.focus();
    if ($input.val().length() > 0) {
      // submit form
    }
  })
  .on('click', 'button.btn-search', function(event) {
    event.preventDefault();
    var $input = $('input');
    $input.focus();
    if ($input.val().length() > 0) {
      // submit form
    }
  })